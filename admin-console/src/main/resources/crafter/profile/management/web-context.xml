<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <!--
    Activates various annotations to be detected in bean classes: Spring's @Required and @Autowired, as well as JSR
    250's @PostConstruct, @PreDestroy and @Resource (if available), JAX-WS's @WebServiceRef (if available), EJB3's @EJB
    (if available), and JPA's @PersistenceContext and @PersistenceUnit (if available).
    -->
    <context:annotation-config/>

    <mvc:annotation-driven/>

    <mvc:resources mapping="/resources/**" location="/resources/"/>

    <mvc:view-controller path="/login" view-name="login"/>

    <bean name="crafter.adminConsoleProperties"
          class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="ignoreUnresolvablePlaceholders" value="true"/>
        <property name="locations">
            <list>
                <value>classpath:crafter/profile/management/server-config.properties</value>
                <value>classpath*:crafter/profile/management/extension/server-config.properties</value>
            </list>
        </property>
    </bean>

    <!-- Controllers -->

    <bean id="crafter.commonControllerAdvice"
          class="org.craftercms.profile.management.web.controllers.CommonControllerAdvice"/>

    <bean id="crafter.profileController" class="org.craftercms.profile.management.web.controllers.ProfileController">
        <property name="defaultStart" value="${crafter.profile.management.paging.start.default}"/>
        <property name="defaultLimit" value="${crafter.profile.management.paging.limit.default}"/>
        <property name="defaultSortBy" value="${crafter.profile.management.paging.sortBy.default}"/>
        <property name="defaultSortOrder" value="${crafter.profile.management.paging.sortOrder.default}"/>
        <property name="tenantService" ref="crafter.tenantServiceRestClient"/>
        <property name="profileService" ref="crafter.profileServiceRestClient"/>
    </bean>

    <bean id="crafter.tenantController" class="org.craftercms.profile.management.web.controllers.TenantController">
        <property name="tenantService" ref="crafter.tenantServiceRestClient"/>
    </bean>

    <!-- Freemarker Config -->
    <bean id="crafter.freemarkerConfig" class="org.springframework.web.servlet.view.freemarker.FreeMarkerConfigurer">
        <property name="templateLoaderPath" value="/WEB-INF/templates/web"/>
    </bean>

    <!--
      View resolvers can also be configured with ResourceBundles or XML files. If you need
      different view resolving based on Locale, you have to use the resource bundle resolver.
    -->
    <bean id="crafter.viewResolver" class="org.springframework.web.servlet.view.freemarker.FreeMarkerViewResolver">
        <property name="contentType" value="text/html;charset=UTF-8"/>
        <property name="cache" value="true"/>
        <property name="prefix" value=""/>
        <property name="suffix" value=".ftl"/>
        <property name="exposeSpringMacroHelpers" value="true"/>
    </bean>

    <import resource="classpath*:crafter/profile/management/extension/web-context.xml"/>

</beans>